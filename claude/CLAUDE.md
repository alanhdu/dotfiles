@~/.agents/AGENTS.md

# Claude Code specifics

Everything above applies to any agent. This section is for capabilities only Claude Code has, so it
must not be moved into the shared `~/.agents/AGENTS.md`.

## Memory

- Never create or edit a memory without my explicit permission.
